# Database (SQLite file path - relative to prisma/schema.prisma location)
# Prisma resolves SQLite paths relative to the schema file, so ../db/ points
# to the project root's db/ folder.
DATABASE_URL="file:../db/custom.db"
